import { NavigationProps } from '@/types/navigation'
import { PlusCircle } from 'lucide-react'

export const navigationRoutes: NavigationProps[] = [
	{ icon: PlusCircle, href: '/generate', label: 'Nova Key' },
]
